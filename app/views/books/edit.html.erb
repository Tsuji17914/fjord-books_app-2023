<h1><%= t('books.editing_book') %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('books.show_this_book'), @book %> |
  <%= link_to t('books.back_to_books'), books_path %>
</div>
