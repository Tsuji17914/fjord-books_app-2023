<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('books.edit.editing_book',model: Book.model_name.human), edit_book_path(@book) %> |
  <%= link_to t('books.back_to_index'), books_path %>
  <%= button_to t('books.destroy',model: Book.model_name.human), @book, method: :delete %>
</nav>
