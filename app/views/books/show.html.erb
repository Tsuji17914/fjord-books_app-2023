<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('books.edit_this_book'), edit_book_path(@book) %> |
  <%= link_to t('books.back_to_books'), books_path %>
  <%= button_to t('books.destroy_this_book'), @book, method: :delete %>
</nav>
